spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/certificate_generator
    username: certificate_generator
    password: certificate_generator
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

app:
  cors:
    allowed-origins: "http://localhost:4200"
  jwt:
    # Local-only placeholder, 256+ bits. Prod reads JWT_SECRET from the environment instead (application-prod.yml).
    secret: "dev-only-secret-key-do-not-use-in-production-32bytes-min"
  admin-bootstrap:
    email: "admin@example.com"
    password: "changeme123"
